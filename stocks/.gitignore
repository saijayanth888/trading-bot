# Trained model weights — reproducible, don't bloat the repo
kb/models/tft/*.pt
# Training summary IS tracked (small JSON, useful for diff)

# Runtime state for the detached ML trainer
memory/stocks-ml-status.json
memory/stocks-ml-train.pid

# Cron logs — append-only, regrow naturally
memory/cron-*.log
